[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "auth-service"
version = "1.0.0"
description = "Multi-Modal AI Content Forensics - Auth Service"
requires-python = ">=3.11"
