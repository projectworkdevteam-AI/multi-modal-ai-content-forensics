[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "api-gateway"
version = "1.0.0"
description = "Multi-Modal AI Content Forensics - API Gateway"
requires-python = ">=3.11"
